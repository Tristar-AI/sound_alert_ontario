# Secrets — keep .env_template tracked as the placeholder
.env
.env.*
!.env_template
!.env.example
!.env.sample

*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
secrets.yaml
secrets.yml
secrets.toml
__pycache__/
.cursor/

# Local virtualenvs — never commit site-packages
venv/
.venv/

# Local alert audio — keep files on disk, do not track
alert_a.wav
alert_b.wav